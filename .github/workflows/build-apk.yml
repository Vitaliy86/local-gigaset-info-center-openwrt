name: Build OpenWrt APK Package

on:
  push:
    branches: [main, master]
    tags: ['v*']
  pull_request:
    branches: [main, master]
  workflow_dispatch:

env:
  PKG_NAME: gigaset-info-center
  PKG_VER: "1.7"

jobs:
  build:
    name: Build .apk Package
    runs-on: ubuntu-latest
    container:
      image: alpine:3.21   # abuild здесь создаёт правильный APKv2

    steps:
      - name: Install build tools
        run: apk add --no-cache alpine-sdk sudo

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup build user (abuild запрещён от root)
        run: |
          adduser -D builder
          addgroup builder abuild
          echo 'builder ALL=(ALL) NOPASSWD: ALL' >> /etc/sudoers
          chown -R builder:builder "$GITHUB_WORKSPACE"

      - name: Generate signing key
        run: su builder -c "abuild-keygen -a -n"

      - name: Build package
        working-directory: ${{ github.workspace }}
        run: su builder -c "abuild -F -P /home/builder/packages"

      - name: Collect artifact
        id: pkg
        run: |
          APK=$(find /home/builder/packages -name '*.apk' | head -1)
          echo "path=$APK" >> "$GITHUB_OUTPUT"
          ls -lah "$APK"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.PKG_NAME }}-apk
          path: ${{ steps.pkg.outputs.path }}
          retention-days: 30

      - name: Create GitHub Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ steps.pkg.outputs.path }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  verify:
    name: Verify Package
    needs: build
    runs-on: ubuntu-latest
    container:
      image: alpine:3.21

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: ${{ env.PKG_NAME }}-apk
          path: /tmp/pkg

      - name: Verify APK is readable by apk-tools
        run: |
          APK=$(find /tmp/pkg -name '*.apk' | head -1)
          echo "Verifying: $APK"
          # Проверяем что пакет валиден для apk-tools
          tar tzf "$APK" | head -20
          tar xzf "$APK" .PKGINFO -O
          echo "✅ Package OK"
